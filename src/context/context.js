import { createContext } from "react";

export const authContext = createContext({})

export const modalContext = createContext({})

export const updateContext = createContext({})

export const updaterModalContext = createContext({})